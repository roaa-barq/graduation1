import 'package:flutter/material.dart';

class changeMap extends StatelessWidget {
  const changeMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}
